// JavaScript has 8 Datatypes
// 1. String 2. Number 3. Bigint 4. Boolean 5. Undefined 6. Null 7. Symbol 8. Object

// The Object Datatype , The object data type can contain:
// 1. An object 2. An array 3. A date

document.addEventListener("DOMContentLoaded", () => {

  /*

    // numbers;
    let num = 43;
    let num2 = 3.5;
    //   strings;
    let name = 'ALI';
    let color = 'BLUE';

    // BOOLEANS
    let ali = true;
    let usman = false;

    // 1. An object
    const person = {firstName: 'ali', lastName: 'zia'};
    console.log(person);

    // 2. An array
    const cars = ['honda', 'bmw' , 'toyota', 'audi'];
    cars.push('volvo');
    console.log(cars);

    // // Date object:
    const date = new Date("May 17, 2024 11:59:10");
    console.log(date);

    // When adding a number and a string, JavaScript will treat the number as a string.
    let x = 16 + "ali";
    console.log(x);

    // JavaScript evaluates expressions from left to right. Different sequences can produce different results:
    let num3 = 5 + 10 + "Ali";  //output is 15Ali;
    console.log(num3);

    // now it will treat all the numbers as string and print as is;;;
    let num4 =  "Ali" + 5 + 10 ; //the result is Ali510;
    console.log(num4);

    // JavaScript Types are Dynamic
    // JavaScript has dynamic types. This means that the same variable can be used to hold different data types.
    let number ; // Now number is undefined.
    number = 44;  // Now number is number.
    number = 'JAVASCRIPT FUNDAMENTALS';  // Now number is string.
    console.log(number);

    // JavaScript Strings
    //A string (or a text string) is a series of characters like "John Doe".
    // Strings are written with quotes. You can use single or double quotes.
    // Using double quotes:
    let carName1 = "Volvo XC60";
    // Using single quotes:
    let carName2 = 'Volvo XC60';

    let answer = "MY NAME IS 'ALIULHAQ'";
    console.log(answer); // the output is === MY NAME IS 'ALIULHAQ';
    let answer2 = 'MY NAME IS "ALIULHAQ"';
    console.log(answer2);  // the output is === MY NAME IS "ALIULHAQ";


  // JavaScript Booleans
  // Booleans can only have two values: true or false.
    let val1 = 122;
    let val2 = 122;
    let val3 = 253;
    let result = (val1 == val2);
    let result2 = (val2 == val3);
    console.log(result , result2);

    // JavaScript Arrays
    // Array items are separated by commas.
  //Array indexes are zero-based, which means the first item is [0], second is [1], and so on.
    const emp = ['ALI', 'IT DEPARTMENT' , 90000];
    console.log(emp);
    // console.log(emp[2]);

  // JavaScript Objects
  // JavaScript objects are written with curly braces {}.
  // Object properties are written as name:value pairs, separated by commas.
  const uni = {department: 'it', salary: 60000 , dutytime : '9 to 5 '};
  console.log(uni);

  // The typeof Operator
  // You can use the JavaScript typeof operator to find the type of a JavaScript variable.
  // The typeof operator returns the type of a variable or an expression:
  let address = 'WAPDA TOWN LAHORE PAK PUNJAB PAKISTAN PHASE 1 ';
  console.log(typeof address);



  // Undefined
  // In JavaScript, a variable without a value, has the value undefined. The type is also undefined.
  let cat;
  console.log(cat);

  let dog = '';
  console.log(typeof  dog);

  ---------------------     */











































  })
